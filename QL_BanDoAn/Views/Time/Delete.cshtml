@model QL_BanDoAn.Models.Time

@{
    ViewBag.Title = "Delete Time";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}

<h2 class="text-center">Delete Time</h2>

<div class="container">
    <h4>Are you sure you want to delete this time?</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            ID
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt class="col-sm-2">
            Value
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Value)
        </dd>
        <!--Thêm các trường khác nếu cần thiết-->
    </dl>

    @using (Html.BeginForm("Delete", "Time", new { id = Model.Id }, FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        <div class="form-group">
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a href="/Time/" class="btn btn-default">Cancel</a>
        </div>
    }
</div>
