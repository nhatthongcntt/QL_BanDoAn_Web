@model QL_BanDoAn.Models.Time

@{
    ViewBag.Title = "Edit Time";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}

<h2 class="text-center">Edit Time</h2>

<div class="container">
    @using (Html.BeginForm("Edit", "Time", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        <div class="form-group">
            @Html.HiddenFor(model => model.Id)
            <label class="control-label">Value:</label>
            @Html.TextBoxFor(model => model.Value, new { @class = "form-control" })
        </div>
        <!--Thêm các trường khác nếu cần thiết-->

        <div class="form-group">
            <input type="submit" value="Save" class="btn btn-primary" />
            <a href="/Time/" class="btn btn-default">Cancel</a>
        </div>
    }
</div>
